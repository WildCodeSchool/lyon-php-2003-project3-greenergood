<div class="form-inline my-2 my-lg-0">
    <div>{{ form_start(form) }}</div>
    {{ form_row(form.search) }}
    <button class="btn btn-outline-light my-2 my-sm-0" type="submit" name="btnSearch"
            onclick="return validate('search_search',2)">
        <svg class="bi bi-search" width="1em" height="1em" viewBox="0 0 16 16" fill="currentColor"
             xmlns="http://www.w3.org/2000/svg">
            <path fill-rule="evenodd"
                  d="M10.442 10.442a1 1 0 0 1 1.415 0l3.85 3.85a1 1 0 0 1-1.414 1.415l-3.85-3.85a1 1 0 0 1 0-1.415z"/>
            <path fill-rule="evenodd"
                  d="M6.5 12a5.5 5.5 0 1 0 0-11 5.5 5.5 0 0 0 0 11zM13 6.5a6.5 6.5 0 1 1-13 0 6.5 6.5 0 0 1 13 0z"/>
        </svg>
    </button>
</div>
{{ form_end(form) }}

<script>
    const message = "Vous devez entrer au minimum 2 caractères";

    function validate(search_search, length) {

        let submit = true;
        let elemLength = document.getElementById('search_search').value.length;

        if (elemLength < length) {
            submit = false;
            alert(message);
        }
        return submit;
    }
</script>